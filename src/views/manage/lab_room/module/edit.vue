<template>
  <div>
    <el-button size="mini" type="success" @click="to">编辑</el-button>
    <eForm ref="form" :sup_this="sup_this" :is-add="false"/>
  </div>
</template>
<script>
import eForm from './form'
export default {
  components: { eForm },
  props: {
    data: {
      type: Object,
      required: true
    },
    sup_this: {
      type: Object,
      required: true
    }
  },
  methods: {
    to() {
      const _this = this.$refs.form
      _this.form = {
        id: this.data.id,
        name: this.data.name,
        number: this.data.number,
        createTime: this.data.createTime,
        build: this.data.build,
        school: this.data.school,
        hold: this.data.hold,
        centerId: this.data.centerId,
        type: this.data.type,
        tel: this.data.tel,
        chargeCourse: this.data.chargeCourse,
        major: this.data.major
      }
      _this.dialog = true
    }
  }
}
</script>

<style scoped>
  div{
    display: inline-block;
    margin-right: 3px;
  }
</style>
